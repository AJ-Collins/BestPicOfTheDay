<div class="max-w-md mx-auto bg-white rounded-lg shadow-md overflow-hidden p-6">
  <h2 class="text-2xl font-bold text-gray-800 mb-4">Login</h2>
  <form action="/auth/login" method="POST">
    <div class="mb-4">
      <label for="email" class="form-label">Email:</label>
      <input type="email" name="email" required class="form-input">
    </div>
    <div class="mb-4">
      <label for="password" class="form-label">Password:</label>
      <input type="password" name="password" required class="form-input">
    </div>
    <button type="submit" class="btn btn-blue w-full">Login</button>
    
    <% if (message) { %>
      <p class="text-red-500 text-sm mt-4"><%= message %></p>
    <% } %>
  </form>
  <p class="mt-4 text-sm text-gray-500">Don't have an account? <a href="/auth/signup" class="text-blue-500">Sign up here</a></p>
</div>
